[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yealink-phonebook-server"
version = "0.1.0"
description = "WebUI + FTP server for Yealink remote phonebooks"
requires-python = ">=3.11"