[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yeabook"
version = "0.1.0"
description = "YeaBook - WebUI for Yealink remote phonebooks over HTTP"
requires-python = ">=3.11"
